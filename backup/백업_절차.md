# TW Project Manager — 백업 절차 (취합본)

> 마지막 정리: 2026-07-21

이 문서는 백업/복원과 관련된 모든 절차를 한곳에 모은 마스터 문서입니다.

---

## 1. 백업의 3가지 대상

| 대상 | 어디에 있나 | 백업 방법 |
|------|-------------|-----------|
| **코드** (index.html) | git 저장소 + 파일 | git commit/tag + backup 폴더 복사 |
| **데이터** (프로젝트 목록) | 각 브라우저 localStorage + 공유 Firebase | Ctrl+S/업로드 시 자동 JSON 다운로드 |
| **복원 안내** | 없음 | 날짜 폴더에 Restore txt 작성 |

- **Firebase**: 가장 마지막에 업로드한 상태 **1개만** 남음 (이력 없음, 팀 공유, last-write-wins).
- **로컬 JSON 백업**: **저장을 누른 사람 컴퓨터**의 다운로드 폴더에만 생성됨 (`tw_pm_backup_날짜_시각.json`).

---

## 2. "백업" 명령 시 절차 (날짜별 폴더 방식)

**원칙: 그날 백업과 관련된 파일은 전부, 예외 없이 `backup/YYYY-MM-DD/` 폴더 하나에만 모은다.**
`backup/` 바로 밑(루트)에는 그 날짜 백업의 파일을 직접 놓지 않는다 — 코드 스냅샷, Restore 안내문, 데이터 JSON까지 전부 같은 날짜 폴더 안에 들어가야 한다. 기존에 이미 있던 다른 날짜 폴더/파일은 그대로 둔다.

1. **날짜 폴더 생성**: `backup/YYYY-MM-DD/`
2. **코드 스냅샷 복사**: `index.html` → `backup/YYYY-MM-DD/index_backup_YYYYMMDD_stable.html`
3. **git 커밋 + 태그** (main 브랜치):
   ```
   git add index.html
   git commit -m "..."          # 그날 작업 요약
   git tag stable-YYYYMMDD
   ```
4. **작업 내역 정리**: 이 문서의 **섹션 7(작업 이력)**에 날짜·작업 목록 추가
5. **Restore 안내 파일 작성**: `backup/YYYY-MM-DD/Restore from a backup_YYYY-MM-DD.txt`
   (아래 항목 포함: 백업 날짜 / git commit / git tag / 백업 파일명 / 코드·데이터 복원법 / 주요 작업 목록)
   → **루트가 아니라 반드시 그 날짜 폴더 안에 작성.**
6. **데이터 JSON도 같은 폴더로**: 사용자가 브라우저에서 **Ctrl+S**를 눌러 생성된 `tw_pm_backup_*.json`을 **같은 `backup/YYYY-MM-DD/` 폴더 안으로** 옮겨 넣으면 그 백업이 완성됨 (코드 + 데이터 + 복원 안내가 폴더 하나에 전부 모임).
   > Claude는 브라우저 데이터에 직접 접근 불가 → 데이터 JSON은 반드시 사용자가 브라우저에서 저장 후 그 폴더로 옮겨야 함.

**완성된 날짜 폴더 안에 있어야 할 파일 (예시, `backup/2026-07-21/`):**
- `index_backup_20260721_stable.html` (코드 스냅샷)
- `Restore from a backup_2026-07-21.txt` (복원 안내)
- `tw_pm_backup_2026-07-21_*.json` (데이터 백업 — 사용자가 옮겨 넣음, 있으면)

---

## 3. 일상 데이터 백업 (자동, 폴더 지정 방식)

- **업로드 버튼(↑) 또는 Ctrl+S** → Firebase 업로드 + **지정 폴더**에 `tw_pm_backup_날짜_시각.json` 자동 저장.
- **저장 폴더 지정 (최초 1회):** 처음 업로드하면 폴더 선택창이 뜸 → `TW_Project_Manager\Upload to Firebase` 폴더 선택. 이후엔 자동으로 그 폴더에 쌓임. 툴바의 **폴더 아이콘 버튼**으로 언제든 변경 가능.
- 형식: `{version, exportedAt, projects, deletedProjects, records, recordMemos}` → 앱의 가져오기(↑)로 복원 가능.
  - `records`: Record 페이지에 저장된 캡처 이미지(base64) 전체. `recordMemos`: 날짜별 메모.
  - 캡처 이미지가 많으면 JSON 파일 용량이 커질 수 있음(이미지가 base64로 그대로 포함됨).
- **Dashboard 전체 캡처**: Dashboard 페이지 상단의 **"전체 캡처"** 버튼 → 5개 하위 페이지(Delivery Acceleration/In Active/준비 중/Resource/Completed)를 자동으로 순회하며 모두 캡처해서 Record 페이지의 **오늘 날짜(기록 기준일) 그룹**에 한 번에 저장. 이 캡처들도 위 JSON 백업(`records`)에 그대로 포함됨.
- 중요한 변경 후에는 **각자 한 번씩 Ctrl+S** 권장 (백업은 누른 사람 로컬에만 생김).
- 브라우저 제약: Chrome/Edge만 폴더 지정 지원. 새 브라우저 세션에서 첫 저장 시 폴더 접근 재확인 프롬프트가 뜰 수 있음(Allow). 미지원/거부 시 다운로드 폴더로 저장.
- 수동 추출 도구: `backup/export_localStorage.html` (Firebase 미연결, localStorage만 읽어 다운로드).

---

## 4. 복원 방법

### 코드 복원
- **Git tag (추천)**: `git checkout stable-YYYYMMDD -- index.html`
- **파일 복사**: `backup/YYYY-MM-DD/index_backup_*.html` → `index.html`로 덮어쓰기

### 데이터 복원
1. 앱을 브라우저에서 연다
2. 가져오기(↑) → 복원할 `tw_pm_backup_*.json` 선택 → 확인
3. **Ctrl+S**로 Firebase에 다시 업로드(팀 공유본 복원)
4. Ctrl+Shift+R 새로고침

---

## 5. 안전장치 (사고 방지)

- Firebase가 비어 있으면(0건) 로컬에 데이터가 있는 한 **덮어쓰지 않음** (실시간 리스너 + 폴링에 가드 적용).
  → 실수로 Firebase가 통째로 비워져도, 로컬이 살아있으면 화면에 유지되고 Ctrl+S로 복원 가능.
- **주의**: 실제 Firebase에 연결되는 index.html을 새 주소(예: localhost 서버)에서 브라우저로 열면 최초 실행 마이그레이션이 Firebase를 비울 수 있음 → 검증 목적으로도 라이브 파일을 브라우저로 띄우지 말 것.

---

## 6. 백업 이력

| 날짜 | git tag | 위치 |
|------|---------|------|
| 2026-06-20 | stable-20260620 | backup/index_backup_20260620_stable.html + Restore txt |
| 2026-06-21 | stable-20260621 | backup/index_backup_20260621_stable.html + Restore txt |
| 2026-07-05 | stable-20260705 | backup/2026-07-05/ (index_backup + Restore txt) |
| 2026-07-11 | stable-20260711 | backup/2026-07-11/ (index_backup + Restore txt) |
| 2026-07-21 | stable-20260721 | backup/2026-07-21/ (index_backup + Restore txt) |

---

## 7. 작업 이력

### 2026-07-21
- **Completed 표 제목에 BU 접두어**: 월례회/이사회/연말 보고 표 제목이 날짜뿐 아니라 BU(IBU/RBU/OBU)로도 구분되도록 그룹핑 방식 변경 (`renderBoardSection`). 같은 날짜라도 BU가 다르면 표가 분리됨.
- **Record 페이지에 날짜별 메모**: 각 날짜 그룹 헤더 아래 메모 입력칸 추가 (`recordMemos`, Firestore/localStorage 동기화).
- **Operational Task 표를 In Active와 완전히 동일한 기능으로 확장**: 진행 단계 체크리스트 사이드패널, Status 배지, 컬럼 다중 필터, 컬럼 리사이즈, IBU/RBU/OBU/Total 탭, 월례회/이사회/연말 보고 + Completed 섹션, 수정 이력 패널 연동까지 In Active 표와 동일한 엔진을 공유하도록 구현 (`findAnyProject` 등 공용 함수 다수를 두 표에서 함께 사용하도록 일반화).
- **차트 개선**: Publication Distribution 차트 폭 확대 + 라벨 글자수 제한 완화, 모든 막대그래프 상단 숫자 잘림 방지(padding 조정), Monthly NEW Count 차트 막대 색상을 빨간색으로 변경.
- **백업 데이터에 Record 캡처 포함**: JSON 백업(`saveLocalBackup`/`exportData`)에 `records`(캡처 이미지)·`recordMemos`(날짜별 메모) 추가. 가져오기(`_doImport`)도 두 필드를 복원하도록 갱신 (구버전 백업 파일과 하위 호환).
- **Dashboard 전체 캡처 기능 추가**: `captureAllDashboardPages()` — Dashboard 5개 하위 페이지를 순서대로 전환하며 각각 캡처해서 Record 페이지의 오늘 날짜 그룹에 저장. Dashboard 페이지 상단에 "전체 캡처" 버튼 추가.
- **백업 절차 정리**: Restore 안내문이 실수로 `backup/` 루트에 만들어졌던 것을 `backup/2026-07-21/`로 이동, 섹션 2에 "관련 파일 전부 날짜 폴더 하나에" 원칙을 명시.

### 2026-07-11
- **버그 수정**: `const DATA_START_Y` 이중 선언 → JS SyntaxError로 앱 전체 먹통 → 제거 및 값 수정
- **Firebase 협업 충돌 방지**: `updatedAt` 기반 per-project 병합, `filterPermDeleted()` 영구삭제 유지
- **영구삭제 복원 방지**: `localStorage` 기반 ID 블록리스트(`tw_pm_perm_deleted_ids`) 적용
- **In Active 차트 교체**: 가로 누적 바 → BU별 세로 막대 그라데이션 SVG 차트
  - Stage 0~3: 공통 회색 계열(`#e2e8f0`→`#b8c2cc`), Stage 4~5: BU 주색으로 전환
  - IBU(`#1E3A8A`) / RBU(`#2563EB`) / OBU(`#2DD4BF`) 각 주색 기준 그라데이션 자동 생성
  - 타이틀 형식: `IBU Total 11`
  - Y축 공유 스케일(globalMax): count 1이면 1칸, 5이면 5칸 비례
  - 숫자 TOP_PAD 20px 추가로 바 위 숫자 잘림 방지
- **단계 산정 로직**: `getPbState()` 기반 5→1 역순 탐색, 마지막 색칠된 단계에만 카운트
- **PPTX 헤더 hRow2**: Progress 컬럼 아래에 1/2/3/4/5 단계 번호 2행 헤더 추가
- **주간 Firebase 자동 백업**: `tw-project-manager-backups` 저장소, 매주 목요일 12시 KST
- **Dashboard 날짜 필터**: Target Date 범위 선택 + Apply 버튼 → Resource Dashboard 필터링

### 2026-07-05
- Firebase 동기화 충돌 해결 (updatedAt 비교 + 5초 블록)
- 07-08 백업 데이터 복원 (force_local 플래그 방식)
- 영구삭제 항목 재등장 방지 초기 구현
